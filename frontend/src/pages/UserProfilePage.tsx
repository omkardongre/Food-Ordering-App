import UserProfileForm from '@/forms/user-profile-form/UserProfileForm';

const UserProfilePage = () => {
  return <UserProfileForm isLoading={false} onSave={() => {}} />;
};

export default UserProfilePage;
